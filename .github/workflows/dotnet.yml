name: test-actions
run-name: ${{ github.actor }} is trying to write his first Action
on: 
  push:
  pull_request:
    branches:
      - main
jobs:
  print-hello:
    runs-on: ubuntu-latest
    steps:
      - run: ::debug::Hello World
  build:
    runs-on: ubuntu-latest
    steps:
      - run: dotnet restore
      - run: dotnet build
  test: 
    runs-on: ubuntu-latest
    steps:
      - run: dotnet test
  publish: 
    runs-on: ubuntu-latest
    steps:
      - run: dotnet publish
